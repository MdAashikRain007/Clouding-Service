spring.application.name=CloudService

#MongoDB Configuration

spring.data.mongodb.uri=mongodb://localhost:27017/cloud
server.servlet.context-path=/api/v1.0
spring.data.mongodb.auto-index-creation=true

#Spring Security Disable default passoword

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

#clerk configuration
clerk.issuer=https://neutral-rattler-47.clerk.accounts.dev
clerk.jwks-url=https://neutral-rattler-47.clerk.accounts.dev/.well-known/jwks.json
clerk.webhook.secret=DCcxSlH3TTPqSDKBBu9FirGqwCRYDODG


# file upload configuration
# allow up to 50MB per file and ensure the request size covers the file limit
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
# (increase both values if you need larger uploads, ensure request size >= file size)

